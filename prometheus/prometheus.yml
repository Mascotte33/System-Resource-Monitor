global:
    scrape_interval: 5s

rule_files: 
  - 'alert_rules.yml'

alerting:
  alertmanagers:
    - static_configs:
      - targets:
        - alertmanager:9093

scrape_configs:
    - job_name: 'system_resource_monitor'
      static_configs:
        - targets: ['host.docker.internal:8000']

